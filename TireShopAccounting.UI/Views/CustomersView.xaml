<UserControl x:Class="TireShopAccounting.UI.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- لیست مشتریان -->
        <GroupBox Grid.Column="0" Header="👥 لیست مشتریان" Margin="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- جستجو -->
                <TextBox Grid.Row="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,15">
                    <TextBox.Resources>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Right">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 جستجو..." 
                                                             Foreground="LightGray" 
                                                             Margin="10,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                
                <!-- DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Customers}" 
                          SelectedItem="{Binding SelectedCustomer}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="نام مشتری" Binding="{Binding Name}" Width="2*"/>
                        <DataGridTextColumn Header="شماره تماس" Binding="{Binding Phone}" Width="*"/>
                        <DataGridTextColumn Header="آدرس" Binding="{Binding Address}" Width="2*"/>
                        <DataGridTextColumn Header="مانده حساب" Binding="{Binding BalanceStatus}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasDebt}" Value="True">
                                            <Setter Property="Foreground" Value="#dc3545"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>
        
        <!-- فرم افزودن/ویرایش -->
        <GroupBox Grid.Column="1" Header="➕ افزودن/ویرایش مشتری">
            <StackPanel>
                
                <Label Content="نام مشتری:"/>
                <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}"/>
                
                <Label Content="شماره تماس:"/>
                <TextBox Text="{Binding CustomerPhone, UpdateSourceTrigger=PropertyChanged}"/>
                
                <Label Content="آدرس:"/>
                <TextBox Text="{Binding CustomerAddress, UpdateSourceTrigger=PropertyChanged}"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         VerticalContentAlignment="Top"
                         Padding="10"/>
                
                <Label Content="مانده حساب (تومان):"/>
                <TextBox Text="{Binding Balance, UpdateSourceTrigger=PropertyChanged}"/>
                
                <!-- دکمه‌ها -->
                <StackPanel Margin="0,30,0,0">
                    <Button Content="💾 ذخیره" 
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"/>
                    
                    <Button Content="➕ جدید" 
                            Command="{Binding AddNewCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                    
                    <Button Content="🗑️ حذف" 
                            Command="{Binding DeleteCommand}"
                            Style="{StaticResource DangerButtonStyle}"/>
                    
                    <Button Content="❌ انصراف" 
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
                
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
